package pt.uminho.taki.api;

/**
 * Enumeração que representa as diferentes funções que um nó pode assumir no sistema.
 *
 * @author TakiLN Team
 * @since 1.0
 */
public enum NodeRole {
    /**
     * Função de nó Central (Global).
     */
    CENTRAL,
    
    /**
     * Função de nó Local (Branch).
     */
    LOCAL
}
